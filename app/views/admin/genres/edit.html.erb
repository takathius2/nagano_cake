<h1>商品ジャンル編集画面</h1>

<%= form_with model: @genre, url: admin_genre_path(@genre.id), method: :patch do |f| %>
    <h3>ジャンル名</h3>
    <%= f.text_field :name %>
    <%= f.submit '決定' %>
<% end %>
    <%= link_to "戻る", admin_genres_path %>